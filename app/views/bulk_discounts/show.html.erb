<h2><%= "Bulk Discount #{@bulk_discount.id}" %></h2>
<p><%= "Quantity Threshold: #{@bulk_discount.quantity_threshold}" %></p>
<p><%= "Percentage Discount: #{@bulk_discount.percentage_discount}" %>
<br>
<br>
<%= link_to "Edit Bulk Discount #{@bulk_discount.id}", "/merchants/#{@bulk_discount.merchant.id}/bulk_discounts/#{@bulk_discount.id}/edit" %>

